TRANSLATE — QUICK START
=======================

WHAT IS THIS?
  A program that walks your translated book through repeated AI editor +
  reviewer rounds until it reads cleanly, then writes the polished chapters
  back out as Word documents.

WHO CAN USE IT?
  Members of the Future Fiction Academy. See LICENSE.txt.

WHAT YOU NEED FIRST
-------------------
  1. Python 3.11 or newer
       - Mac:     download from https://www.python.org/downloads/
       - Windows: download from https://www.python.org/downloads/
                  (during install, tick "Add Python to PATH")
       - Linux:   sudo apt install python3.11 python3.11-venv      (Debian/Ubuntu)
                  sudo dnf install python3.11                       (Fedora)
                  sudo pacman -S python                             (Arch)

  2. At least ONE way to reach an AI (either works; you can use both):

     a) An AI subscription you already pay for, through its command-line
        tool, signed in on this computer:
          - Claude Max         -> the "claude" CLI (Claude Code)
          - ChatGPT Plus/Pro   -> the "codex" CLI
          - Gemini AI Pro      -> the "gemini" CLI
          - Qwen (free)        -> the "qwen" CLI
        Rounds routed this way cost $0 extra — your subscription covers them.
        See README.md for install + sign-in commands per CLI.

     b) An OpenRouter account and API key
          - Sign up at https://openrouter.ai
          - Click "Keys" in the top-right, copy a key, keep it secret
          - You pay OpenRouter per call, but you get the full model list
            (hundreds of models, with prices shown in the app).

HOW TO START IT
---------------

  Mac:
    Double-click "Launch Translate.command"
      (the first time you do this, macOS may say "cannot be opened because
       it is from an unidentified developer". If so: right-click the file
       once, choose "Open", confirm. After that, double-click works.)

  Windows:
    Double-click "Launch Translate.bat"

  Linux:
    Open a terminal in this folder and run:
      ./launch-translate.sh
    (or right-click in your file manager and choose "Run in Terminal".)

A Terminal/Console window opens, the first run installs dependencies
(~30 seconds), and your default browser opens to the Translate app.

KEEP THE TERMINAL WINDOW OPEN while you work. Closing it stops the program.

FIRST TIME IN THE APP
---------------------
  1. Click "Settings" (top-right). Any AI CLIs signed in on this computer
     show as "detected" with a green dot.
  2. Using OpenRouter too (or instead)? Paste your API key, click Save,
     then "Refresh model list".
  3. Pick a default Editor model: choose a provider in the selector
     (a detected CLI, or OpenRouter), then pick from the model dropdown —
     it shows writing scores, context sizes, and prices.
  4. Pick a different default Reviewer model the same way. Save.
  5. Click "← Books".
  6. Click "+ New book". Pick your translated .docx and the English
     original, choose languages, click "Create book".
  7. Click any chapter row.
  8. Click "Continue" — Translate sends one round (Editor → Reviewer →
     apply suggestions) and shows progress in the activity log.
  9. When the chapter looks good, click "Done". A "final.docx" lands
     in ~/.translate/<your-book>/chapters/chNN/

For step-by-step screenshots and explanations of every button, see the
included README.md.

STOPPING IT
-----------
  Press Ctrl+C in the Terminal window, or close that window.

WHERE IS MY WORK SAVED?
-----------------------
  Mac:     ~/.translate/<book-name>/
  Windows: %USERPROFILE%\.translate\<book-name>\

  Each chapter gets its own folder with every round saved separately and
  a final.docx once you click Done.

TROUBLESHOOTING
---------------
  See README.md ("Troubleshooting") — including what "not found" means in
  the provider list and the daily cap on Codex rounds.

© 2026 GML Publishing LLC. Restricted MIT License — see LICENSE.txt.
